<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pterodactyl Node Status</title>
    <style>
        /* General Body and Layout Styles */
        body {
            font-family: Arial, sans-serif;
            background-color: #2d2d2d;
            color: white;
            margin: 0;
            padding: 0;
            overflow-x: hidden;
            animation: backgroundAnimation 8s ease infinite;
        }
        h1 {
            text-align: center;
            padding: 20px;
            animation: fadeIn 1s ease-in-out;
        }

        /* Background Animation */
        @keyframes backgroundAnimation {
            0% {
                background-color: #2d2d2d;
            }
            50% {
                background-color: #1e1e1e;
            }
            100% {
                background-color: #2d2d2d;
            }
        }

        /* Form and Box Animation */
        .form-container {
            max-width: 400px;
            margin: auto;
            padding: 20px;
            background-color: #444;
            border-radius: 8px;
            opacity: 0;
            animation: formSlideIn 1s forwards ease-in-out;
        }
        @keyframes formSlideIn {
            from {
                transform: translateY(-30px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        /* Input Fields Animation */
        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 4px;
            border: 1px solid #ccc;
            background-color: #333;
            color: white;
            animation: inputFocus 1s ease-out infinite alternate;
        }
        @keyframes inputFocus {
            0% {
                background-color: #333;
            }
            100% {
                background-color: #555;
            }
        }

        button {
            width: 100%;
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: all 0.3s ease-in-out;
        }
        button:hover {
            transform: scale(1.1);
            background-color: #45a049;
        }

        /* Loading Spinner */
        .loader {
            border: 8px solid #f3f3f3;
            border-top: 8px solid #3498db;
            border-radius: 50%;
            width: 50px;
            height: 50px;
            animation: spin 2s linear infinite;
            margin: 0 auto;
        }

        @keyframes spin {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }

        /* Node List Styles */
        .node-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            animation: fadeIn 2s ease-in-out;
        }

        .node-card {
            width: 250px;
            margin: 10px;
            padding: 20px;
            border-radius: 8px;
            background-color: #444;
            opacity: 0;
            animation: nodeCardSlideIn 1s forwards ease-in-out;
        }

        @keyframes nodeCardSlideIn {
            from {
                transform: translateY(30px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .error {
            color: red;
            text-align: center;
        }

        /* Status Styles */
        .online {
            color: #4CAF50;
        }

        .offline {
            color: #f44336;
        }

        .maintenance {
            color: #f39c12;
        }
    </style>
</head>
<body>
    <h1>Pterodactyl Panel Node Status</h1>

    <div class="form-container">
        <h2>Enter Your Panel URL and API Key</h2>

        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <form action="/" method="POST">
            <label for="panel_url">Panel URL:</label>
            <input type="text" id="panel_url" name="panel_url" placeholder="https://panel.yourdomain.com" required>

            <label for="api_key">API Key:</label>
            <input type="password" id="api_key" name="api_key" placeholder="API Key" required>

            <button type="submit">Verify</button>
        </form>
    </div>

    <% if (loading) { %>
        <div class="loader"></div>
    <% } %>

    <% if (nodes && nodes.length) { %>
        <h2>Node Status</h2>
        <div class="node-list">
            <% nodes.forEach(node => { %>
                <div class="node-card">
                    <h2><%= node.attributes.name %></h2>
                    <p><strong>Description:</strong> <%= node.attributes.description %></p>
                    <p><strong>Memory:</strong> <%= node.attributes.memory %> MB</p>
                    <p><strong>Disk:</strong> <%= node.attributes.disk %> GB</p>
                    <p><strong>Status:</strong>
                        <% if (node.status === 'Maintenance') { %>
                            <span class="maintenance">Maintenance Mode</span>
                        <% } else if (node.status === 'Online') { %>
                            <span class="online">Online</span>
                        <% } else { %>
                            <span class="offline">Offline</span>
                        <% } %>
                    </p>
                    <p><strong>FQDN:</strong> <%= node.attributes.fqdn %></p>
                </div>
            <% }) %>
        </div>
    <% } %>

</body>
</html>
