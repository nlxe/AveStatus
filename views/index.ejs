<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Node Status</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f0f8ff;
            color: #333;
        }

        .christmas-header {
            background-color: #f44336;
            color: white;
            text-align: center;
            padding: 15px;
            font-size: 24px;
        }

        .christmas-footer {
            background-color: #f44336;
            color: white;
            text-align: center;
            padding: 15px;
            position: absolute;
            width: 100%;
            bottom: 0;
        }

        .christmas-greeting {
            font-size: 30px;
            font-weight: bold;
        }

        .snowflakes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        .snowflake {
            position: absolute;
            top: -10px;
            font-size: 2em;
            animation: snow 10s linear infinite;
        }

        @keyframes snow {
            0% {
                transform: translateY(-10px);
            }

            100% {
                transform: translateY(100vh);
            }
        }

        .node-status-container {
            text-align: center;
            margin: 20px;
        }

        .node-list {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        .node-item {
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            margin: 15px;
            padding: 20px;
            width: 250px;
            text-align: left;
            transition: transform 0.3s ease;
        }

        .node-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .node-item h2 {
            color: #f44336;
            font-size: 18px;
            margin-bottom: 10px;
        }

        .node-item p {
            font-size: 14px;
            margin: 5px 0;
        }

        .node-specs,
        .node-uptime {
            background-color: #f4f4f4;
            padding: 10px;
            border-radius: 4px;
            margin-top: 10px;
        }

        .node-specs p,
        .node-uptime p {
            font-weight: bold;
            color: #555;
        }

        .node-specs span,
        .node-uptime span {
            color: #f44336;
        }

        footer .snowflake {
            font-size: 1.5em;
        }
    </style>
</head>

<body>
    <header class="christmas-header">
        <span class="christmas-greeting">🎄 Merry Christmas 🎄</span>
    </header>

    <div class="snowflakes">
        <div class="snowflake" style="left: 10%;">❄️</div>
        <div class="snowflake" style="left: 25%;">❄️</div>
        <div class="snowflake" style="left: 40%;">❄️</div>
        <div class="snowflake" style="left: 60%;">❄️</div>
        <div class="snowflake" style="left: 80%;">❄️</div>
    </div>

    <div class="node-status-container">
        <h1>Node Status</h1>
        <div class="node-list">
            <% nodes.forEach(function(node) { %>
                <div class="node-item">
                    <h2><%= node.name %></h2>
                    <p>FQDN: <%= node.fqdn %></p>
                    <div class="node-specs">
                        <p>Memory: <span><%= node.memory %> MB</span></p>
                        <p>Disk: <span><%= node.disk %> GB</span></p>
                        <p>Location: <span><%= node.location %></span></p>
                        <p>Servers Count: <span><%= node.servers_count %></span></p>
                    </div>
                    <div class="node-uptime">
                        <p>Created at: <span><%= new Date(node.created_at).toLocaleString() %></span></p>
                        <p>Status: <span><%= node.status %></span></p>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>

    <footer class="christmas-footer">
        <p>Made with ❤️ by Your Name</p>
    </footer>
</body>

</html>
